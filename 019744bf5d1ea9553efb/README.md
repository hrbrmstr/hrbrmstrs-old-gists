Add this to `~/.R/snippets/r.snippets`

Use with `figs<TAB>` to insert into a doc

CAVEAT!

- Store images in `man/figures`
- To save some headache for PDF manual generation, don't use `_` characters in the filenames